"""Una tupla es como una lista, pero inmutable (no puedes modificar sus elementos una vez creada)."""

coordenadas = (10, 20)

print(coordenadas[0])

# Desempaquetando
x, y = coordenadas

print(f"X={x}, Y={y}")
